{
  "name": "@loop/export-archon",
  "version": "0.1.0",
  "description": "Optional interop: compile a Loop loop-spec to an Archon workflow YAML file. Loop runs natively on Claude Code; this is for teams already running Archon.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": { ".": "./dist/index.js" },
  "files": ["dist", "src"],
  "dependencies": {
    "@loop/parser": "0.1.0"
  },
  "devDependencies": {
    "yaml": "2.4.5"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test test/*.test.js"
  }
}
