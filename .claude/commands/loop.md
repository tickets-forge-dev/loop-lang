---
description: Create or run a Loop (.loop) flow in this session
---

Use the **loop** skill for this request: $ARGUMENTS

- If the argument names a `.loop` file (or a path), **run it natively in this session** —
  walk plan → act → observe, reflect on failure and re-plan, honor each `done when` check
  and `after N tries` guard, and pause for any human gate (`a human approves…`) by asking
  me right here.
- If the argument describes work to do ("fix the auth test, gate the deploy"), **create a
  `.loop` file** for it first using the grammar, show it, then offer to run it.

Narrate each step so I can watch the loop run.
